const AUTH = '/auth';
const POSTS = '/posts';
const REGISTER = '/api/auth/register';
const LOGIN = '/api/auth/login';
const GETPARKING = '/api/parking/get-parkings'

export default {
  AUTH,
  POSTS,
  REGISTER,
  LOGIN,
  GETPARKING
} as const;
